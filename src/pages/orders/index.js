//Order page to show the list of orders
import OrderList from "@/components/OrderList";

const OrdersPage = () => {
  return (
    <div>
      <OrderList />
    </div>
  );
};

export default OrdersPage;
